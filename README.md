# ViT-PyTorch
In this repository, i attempt to implement, using the PyTorch framework, a vision transformer for image classification and, pherhaps, other applications. I also plan to implement other types of ViT such as swin-transformers, which form the basis for many SOTA networks.
